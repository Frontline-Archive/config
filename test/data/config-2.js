'use strict';

/**
 *
 * Sample config for testing
 * 
 */

let config = {
	'consul' : {
		'host' : '127.0.0.1',
		'port' : 8500
	},

	'version' : '1.0.0',

	'log' : {
		'console' : {
			'level' : 'info'
		}
	}
};

module.exports = config;